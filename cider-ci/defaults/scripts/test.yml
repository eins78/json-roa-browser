### test ##################################################

integration-tests:
  body: |
    #!/usr/bin/env bash
    set -eux
    sleep 5 && true
  start-when:
  - script: start-vnc
  - script: api-is-running
  - script: run-selenium
    states: [executing]
